matrix:
  include:
    - language: node_js
      node_js:
        - 10
      before_install:
        - cd client
      install:
        - npm install 
      script:
        - npm test
    
    # - language: ruby
    #     ruby: 
    #       - 2.4
    #     before_install:
    #       - cd server
    #     script:
    #      - rails s -p 3001
